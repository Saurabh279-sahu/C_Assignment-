//Print Character of Given ASCII Value
#include <stdio.h>

int main() {
    int ascii;

    printf("Enter ASCII value: ");
    scanf("%d", &ascii);

    printf("Character = %c", ascii);

    return 0;
}